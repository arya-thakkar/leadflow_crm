[build]
  command = "npm run build"
  publish = "build"
  environment = { NODE_VERSION = "20", REACT_APP_BACKEND_URL = "<YOUR_BACKEND_URL>" }

# Redirect all routes to index.html for client‑side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
